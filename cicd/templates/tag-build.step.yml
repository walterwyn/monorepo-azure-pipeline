parameters:
  projectName: ''

steps:
  - bash: |
      git tag build-$PROJECT_NAME-$BUILD_BUILDID
      git push --tags
    displayName: Tag build
    condition: succeeded()
    env:
      PROJECT_NAME: ${{ parameters.projectName }}
